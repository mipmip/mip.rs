## ADDED Requirements

### Requirement: Frontmatter flag
The system SHALL accept a `--frontmatter` flag that enables frontmatter display.

#### Scenario: Frontmatter flag accepted
- **WHEN** the user runs `mip --frontmatter <file>`
- **THEN** the system SHALL start normally with frontmatter display enabled
